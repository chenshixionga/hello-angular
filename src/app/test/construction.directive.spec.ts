import { ConstructionDirective } from './construction.directive';

describe('ConstructionDirective', () => {
  it('should create an instance', () => {
    const directive = new ConstructionDirective();
    expect(directive).toBeTruthy();
  });
});

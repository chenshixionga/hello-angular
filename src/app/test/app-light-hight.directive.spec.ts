import { AppLightHightDirective } from './app-light-hight.directive';

describe('AppLightHightDirective', () => {
  it('should create an instance', () => {
    const directive = new AppLightHightDirective();
    expect(directive).toBeTruthy();
  });
});
